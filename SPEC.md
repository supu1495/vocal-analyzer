# SPEC.md — 要件定義・設計方針

vocal-analyzerの製品要件・設計上の決定事項・将来方針をまとめたドキュメントです。
外部向けのREADME.mdとは分けて、開発者視点の詳細を記録します。

---

## プロダクトの目的

カラオケ録音をAIで分析し、歌唱力向上のための具体的なフィードバックを提供する。
ボイストレーナーがいない環境でも、客観的な指標で自分の歌唱を振り返れるようにする。

### ターゲットユーザー

- カラオケを趣味として楽しむ人
- 歌唱力を向上させたい人
- ボイストレーニングの補助ツールとして使いたい人

### 開発の背景・既存システムへの問題意識

既存のカラオケ採点システムはMIDIデータとの波形一致を重視する「音ゲー」的なアプローチを取っており、**楽曲本来の表現や音楽的な歌唱クオリティを正しく評価できていない**。このプロダクトはその課題を解決するために構築する。

**既存システムの具体的な問題点（Anti-Patterns）:**

- **ハードウェア依存** — カラオケ機器の機種（DAM・ジョイサウンド等）やマイク設定によって録音品質が変動し、スコアが影響を受ける。根本解決は困難だが、録音時に「音量・距離・ノイズ」を視覚的にガイドするUI機能があると誤入力を減らせる（将来対応）
- **「棒読み」の過大評価** — 機械的な音程維持（棒読み）が高評価される。楽曲の文脈に沿わない不自然な安定は評価を下げるべき
- **表現技法の回数主義** — 「しゃくり」「こぶし」等を回数だけで加点評価している。フレーズ内での密度や適切さで判断すべき

---

## 機能要件

### 実装済み（Phase 1〜6）

| 機能 | 詳細 |
|---|---|
| ユーザー認証 | メール・パスワード登録・ログイン。JWT（httpOnly Cookie）方式 |
| ロックアウト | 5回連続ログイン失敗で15分ロック（Redis） |
| 音声アップロード | WAV / MP3 / M4A、最大50MB |
| ピッチ分析 | Crepeによるフレームごとのピッチ検出・安定性スコア（0〜100） |
| 分析結果保存 | PostgreSQLに保存。音声ファイルは即時削除 |
| 統計ダッシュボード | 過去の分析結果の推移グラフ |
| バックエンドテスト | pytest による認証・認可・分析APIのテスト（34テスト） |

### 実装予定

| フェーズ | 機能 | 備考 |
|---|---|---|
| Phase 7 | 歌唱技法検出 | ✅ 実装済み（しゃくり・ロングトーン・フォール・こぶし・ビブラート）。スコアマトリクス連携待ち |
| Phase 7 | リズム・グルーブ感評価 | ✅ 実装済み（onset×ビートオフセット一貫性）。スコアマトリクス連携待ち |
| Phase 7 | 声域計算 | ✅ 実装済み（最低音・最高音・半音数）。スコアマトリクス連携待ち |
| Phase 7 | スコアマトリクス実装 | 未実装。総合スコア＋3軸の詳細スコア。DBカラム追加が伴う |
| Phase 7 | ルールベースフィードバック生成 | 未実装。スコアに基づくテンプレート文章。将来LLMに差し替える設計にする |
| Phase 8 | 非同期処理（Celery） | Demucs復帰の前提 |
| Phase 8 | Demucs音源分離 | CPUが遅いためCelery実装後に本番復帰 |

### 将来検討（優先度低・時期未定）

| 機能 | 備考 |
|---|---|
| おすすめ楽曲提案 | 声域・スタイルに合った楽曲提案。楽曲データの取得元（Spotify APIなど）と推薦ロジックの設計が必要。他の将来機能より優先度高め |
| 非ログインでの分析 | ゲストユーザーや友人の分析を代行するユースケースを想定。user_id NOT NULL化と連動 |
| JWTブラックリスト | ログアウト後のトークン無効化。現在はhttpOnly CookieでXSS対策済みのため後回し |
| 声紋登録・比較 | usersテーブルにvoiceprint_dataカラムあり。機能実装は未着手 |
| 楽曲構造検出（A/Bメロ・サビ） | librosa.segmentで区間分割は可能だが精度が低い。歌詞・参照データが用意できたタイミングで実装。歌声と話し声の区別にも活用できる |
| LLMフィードバック | ルールベース実装後に差し替える。APIコストが許容できるタイミングで導入 |
| PC版UI | 現状はスマートフォン向けレイアウト。PC向けレスポンシブ対応またはPC専用レイアウトを追加する |
| 複数ファイル一括アップロード・日付指定 | 複数の音声ファイルを一度に投下する機能、および録音日時を手動で指定して登録する機能 |
| アクセント・ハンマリング検出 | DAM AI HEART を参考に追加予定の歌唱技法。Phase 7完了後に検討 |
| 分析進捗表示（プログレスバー・残り時間） | 録音データ解析中の残り時間または進捗バーをUIに表示。Phase 8（Celery非同期）と連動して実装 |
| 録音ガイドUI | アップロード前に音量・距離・ノイズを視覚的にチェックできる機能。カラオケ機器の機種差による録音品質のばらつきを減らす目的 |
| 話者分離・歌声検出のテスト発声 | 歌声と話し声の判別、および複数人が映り込んだ録音から対象話者の声だけを抽出する目的。分析前に短いテスト発声を行い声のプロファイルを取得することで、カラオケで他の人が一緒に歌っている場合や背景の声を除外できる。楽曲構造検出（A/Bメロ・サビ）とも連動する可能性あり |

---

## コア評価ロジックの設計方針

### 基本思想

楽曲ごとの「理想的な歌唱モデル」との比較ではなく、**歌唱そのものの音楽的整合性を判別する汎用ロジック**を構築する。単純な加点方式ではなく、各要素をプロットした上で最終的な「歌唱特性」を結論づける。

### A. スコアマトリクス（多角的評価）

**UI表示方針：** 総合スコア1つをメインで表示。詳細ボタンで各軸の内訳を確認できる。

| 評価軸 | 内容 | 加点 / 減点 |
|---|---|---|
| 基本忠実度 | 音程・リズムの正確性 | ベースライン（加点） |
| 表現の不自然さ | 不自然に連続する技法、抑揚のないロングトーン（棒読み） | 減点要素 |
| 音楽的ダイナミクス | ブレスのタイミング、声の質感の変化（A/Bメロ・サビ対比は将来対応） | 加点要素 |

**棒読み検出の方針（Approach A）：**
- フレーズ内のピッチ分散が極端に小さい
- かつ技法検出数がゼロ
- → 「不自然な安定」として減点。実際にテスト・検証しながら閾値を調整する

### B. 技法密度分析（Technique Density Analysis）

1小節またはフレーズ単位で技法の出現回数をカウントし、一定の閾値を超えた場合は「表現の過剰」として評価を調整する。技法を「使っているかどうか」ではなく「適切な密度で使えているか」で評価する。

**具体例：** バラードのAメロで「しゃくり」を連発する → 「表現の不自然さ」として減点。技法はスパイスであり、使いすぎは評価を下げる。

### C. テキストベースのフィードバック

抽象的な点数だけでなく、分析結果に基づいた具体的なテキストを出力する。出力形式：

- **歌唱特性** — 分析から導き出されたその人の歌い方の傾向
- **改善点** — 具体的に直すべきポイント
- **伸ばすべきポイント** — 現時点での強みと磨き方

**実装方針：**
ルールベーステンプレートで実装する（コスト0・安定動作）。フィードバック生成を独立した関数として切り出し、将来LLMに差し替えられる設計にする。LLM導入はAPIコストが許容できるタイミングで検討。

### 抽出する音響特徴量

| 特徴量 | ライブラリ | 備考 |
|---|---|---|
| ピッチ（F0） | Crepe | フレームごとの検出・信頼度付き |
| フォルマント | librosa | 声質・母音の分析に使用 |
| エネルギー（音量） | librosa | ダイナミクス・ブレス解析に使用 |
| BPM・リズム | librosa | テンポ検出・タイミングのズレ計測 |
| 歌唱技法 | 独自実装 | ビブラート・こぶし・フォール・しゃくり・ロングトーン |

---

## 非機能要件

### プライバシー・著作権

- **音声ファイルは一切保存しない**（著作権保護のため）
- 分析結果（数値・JSON）のみPostgreSQLに保存
- アップロードされた一時ファイルは分析完了後に即時削除

### セキュリティ

- JWT認証はhttpOnly Cookieで管理（XSSによるトークン窃取を防止）
- パスワードはbcryptでハッシュ化
- ログイン失敗のロックアウトはRedisで管理
- エラーメッセージはユーザー列挙攻撃を防ぐため曖昧化（「メールアドレスまたはパスワードが正しくありません」）
- 本番環境ではHTTPS必須
- 本番環境ではDBパスワード（`POSTGRES_PASSWORD`）をハードコードせず、AWS Secrets Managerなどのシークレット管理ツールまたは環境変数で注入すること（現在の `docker-compose.yml` は開発専用のハードコード値 `postgres` を使用）
- 本番環境では `.env.example` の `DATABASE_URL` / `REDIS_URL` を実際の接続先に差し替えること（現在は開発用のデフォルト値がそのまま書かれているため、本番デプロイ時には環境変数またはシークレット管理ツールで注入する）

### パフォーマンス

- Demucsは処理が重いため非同期化（Celery）が前提
- Crepeはピッチ検出精度優先（librosa.pyinへの変更はしない）

---

## 設計上の決定事項

### analysis_results.user_id を Nullable のまま維持する理由

現状は`NOT NULL`化せずNullableを維持する。
将来的に「ログインなしで分析できる機能」（ゲスト分析・友人の分析代行など）を実装する予定があるため、NOT NULL化はそのタイミングまで保留。

### Demucsのスタブ化

CPUでの処理が遅すぎるため、Celery非同期処理の実装後に本番復帰させる。
現在は`audio/separator.py`がlibrosaで音声をそのままボーカルとして返すスタブ実装。

### CrepeをTensorFlowで動かす

ピッチ検出の精度重視のためlibrosa.pyinへの変更はしない。
hmmlearnとpybind11の競合を回避するため、Dockerfileで`crepe`を`--no-deps`でインストールする。

### JWTブラックリストを後回しにする理由

httpOnly CookieによりXSSでのトークン窃取リスクは軽減済み。
完全なセキュリティ対策（ログアウト後の即時無効化）は本番デプロイ前のタイミングで実装する。

---

## データモデル概要

### users テーブル

| カラム | 型 | 備考 |
|---|---|---|
| id | INTEGER PK | |
| email | VARCHAR(255) UNIQUE NOT NULL | |
| hashed_password | VARCHAR(255) NOT NULL | デフォルト'' → 将来的に要見直し |
| voiceprint_data | JSON nullable | 声紋データ（機能未実装） |
| disclaimer_accepted | BOOLEAN NOT NULL DEFAULT FALSE | |
| created_at | TIMESTAMP | |

### analysis_results テーブル

| カラム | 型 | 備考 |
|---|---|---|
| id | INTEGER PK | |
| user_id | INTEGER nullable | ゲスト分析機能実装まではNullable維持 |
| pitch_accuracy | FLOAT nullable | 0〜100 |
| rhythm_score | FLOAT nullable | 0〜100（現在スタブ） |
| techniques | JSON nullable | 技法検出結果 |
| vocal_range | JSON nullable | 最低音・最高音（現在スタブ） |
| feedback | TEXT nullable | AIフィードバック（現在スタブ） |
| created_at | TIMESTAMP | |

---

## AI活用の方針

このプロダクトはClaude Codeを活用して開発している。以下は開発を通じて大切にする原則。

| | 原則 | 具体的な行動 |
|---|---|---|
| 🔍 | **自信満々な回答ほど疑う** | 数値・固有名詞・APIは必ず公式ドキュメントで確認する。AIは「たぶん」という感覚でも断言することがある |
| ✂️ | **一度に大きな作業をさせない** | 「全部やって」より「まず調べて→次に提案→最後に実装」と段階を分ける |
| 📎 | **コンテキストを惜しまず渡す** | エラー文・実際のコード・バージョン情報を渡す。「なんかうまくいかない」はNG |
| ↩️ | **おかしいと思ったら即突っ込む** | 「本当にそのメソッドある？」と聞き返すだけで正直に訂正してくれることが多い |
| ⏸️ | **承認フローをサボらない** | Claude Codeの確認ステップは面倒でも飛ばさない。AIの暴走を防ぐブレーキ |
| 🧠 | **「答えを出す機械」として使わない** | 一緒に考えるパートナーとして使う。最終判断は常に自分がする |
| ⚖️ | **設計レビューは丁寧に、実装は一気に出させる** | 設計・方針の確認は段階を踏んで慎重に行い、合意が取れた実装は一気に書かせる。速さと安全のバランスとして最も現実的なアプローチ |

---

## 既知の問題・技術的負債

| 問題 | 重大度 | 対応予定 |
|---|---|---|
| ロギング未実装 | 🟡 低 | Phase 7以降 |
| `docker-compose.yml` に healthcheck 未設定 | 🟡 低 | Phase 8以降 |
| しゃくり検出の `avg_height` 過小評価（ゆっくり上昇スタイル） | 🟡 低 | 将来対応 |
| 200セント超の音程変化を未分類のまま除外している | 🟡 低 | 将来対応 |
| 有声区間内のしゃくり（無声区間をはさまない滑り込み）を未検出 | 🟡 低 | 将来対応 |
| 大振幅ビブラートがフレーズ末尾まで続く場合、最後の下降半周期をフォールと誤検出する可能性 | 🟡 低 | ビブラート検出実装後に対応 |
| フレーズ末尾で上昇して終わるこぶし（逆V字）を未検出 | 🟡 低 | 将来対応 |
| ビブラートの「加点目的」と「アーティストのアレンジ模倣」を区別できない | 🟡 低 | 将来対応 |

#### 補足

**しゃくりの `avg_height` 過小評価：**  
`_SHAKURI_SETTLE_FRAMES`（5フレーム）以内にゆっくり上昇するスタイル（藤井風のようなグルーヴ重視のアーティスト）では、安定音程の推定に上昇中のフレームが混入して `avg_height` が実際より小さく出る。検出の有無には影響しないが正確な幅の計測には改善が必要。

**有声区間内のしゃくり：**  
現在の実装は「無声（NaN）→ 有声」の切り替わり時のみしゃくりを検出する。母音が連続するメリスマや有声子音（n, r など）が続くフレーズでは NaN が生まれないため、その区間内のしゃくりを取りこぼす。有声区間内の「ピッチ下降 → 上昇パターン」を検出するにはピッチのスムージングが必要で、実音声によるパラメータ調整が先決のため現段階では対応しない。

**ビブラートの意図識別不能：**  
「旋律のない区間でのビブラート」を減点したい一方で、アーティストのビブラートスタイルを模倣した表現は加点したい。しかし、「採点目的の加点行為」と「意図的なアレンジ模倣」は音声データだけでは区別できない。どこにビブラートをかけるべきかという文脈判断には楽曲のリファレンスデータが必要なため、現フェーズでは `detect_vibrato` の返り値から `gratuitous_count` フィールドを除外している。将来的にはLLMフィードバックやリファレンス楽曲との比較で対応する。

**フレーズ末尾の上昇こぶし未検出：**  
`detect_kobushi` はこぶしの「ベースラインへの戻り」を確認するために逸脱区間の後にフレームが必要なため、有声区間の末尾に達した逸脱はスキップしている。下降方向の末尾逸脱は `detect_fall` が担当するが、上昇方向（逆V字）の末尾こぶしはいずれにも検出されない。対応方針として、逸脱が上昇方向かつ有声区間の末尾に達した場合は「戻り確認なしのこぶし候補」として低信頼度フラグ付きで記録する、または隣接する有声区間の開始ピッチをもって「戻り」とみなす方法が考えられる。

**大振幅ビブラートのフレーズ末尾でのフォール誤検出：**  
フォール検出は「有声 → 無声（NaN）への切り替わり直前の単調下降区間」を対象にしている。ビブラートが大きな振幅（±100cent以上）でフレーズ末尾まで続いた場合、最後の下降半周期（約60ms・100cent程度）が深さ・時間・速度の全フィルタを通過してフォールとして誤検出される可能性がある。単調性ウォークにより拾う区間を1サイクル分に絞っているため影響は限定的。根本解決はビブラート検出（`detect_vibrato`）の実装後に「ビブラート区間をフォール検出から除外する」後処理を加えることで対応する。

**200セント超の音程変化：**  
`_SHAKURI_MAX_CENTS = 200.0` を超える上昇をしゃくりから除外しているが、原因は複数考えられる。体力が続かず音程をキープできていないケース、感情的な荒々しさや意図的な崩しの表現である可能性もある。現段階では未分類のまま除外しており、将来は「感情表現強度」の別指標として検出できると望ましい。

### 解決済み

| 問題 | 解決内容 |
|---|---|
| `GET /analysis/{id}` に認証チェックがない | `Depends(get_current_user)` + `user_id` 所有権チェックを追加（Phase 5.5） |
| タイミング攻撃によるユーザー列挙の可能性 | `DUMMY_HASH` をモジュールロード時に生成し、ユーザー不在でも `verify_password` を実行する（Phase 5.5） |
| ロックアウトのRedis操作が非atomic | `INCR + EXPIRE` をLuaスクリプトでatomicに実行するよう変更（Phase 5.5） |
| `python-jose` にCVE-2024-33663が報告済み | `PyJWT==2.12.1` へ切り替え（Phase 6） |
| passlib が Python 3.13 の `crypt` モジュール削除に非対応 | passlib を削除し `bcrypt` を直接使用するよう変更（Phase 6） |

## Phase 6 テスト計画

音声分析の精度検証（Crepe・librosa）はスタブ段階のため対象外。フレームワーク組み込みのバリデーション（422エラー）も対象外。**認証・認可・入力バリデーション・正常系の主要フローを優先する。**

### `auth_utils.py` ユニットテスト（DBなし・純粋関数）

| テスト対象 | テスト内容 |
|---|---|
| `hash_password` | 元のパスワードと異なる文字列が返るか |
| `verify_password` | 正しいパスワードで `True`、誤りで `False` が返るか |
| `create_access_token` | JWT文字列が生成されるか。デコードして `sub`（user_id）と `exp` が含まれるか |
| `_decode_token` | 正常なトークンからuser_idが取り出せるか（**PyJWT移行後の動作確認を兼ねる**） |
| `_decode_token` | 改ざんしたトークンで `401` が返るか |
| `_decode_token` | 有効期限切れトークンで `401` が返るか |

### `auth_utils.py` ユニットテスト（Redisモック使用）

| テスト対象 | テスト内容 |
|---|---|
| `check_lockout` | 失敗カウントが5未満では例外が発生しないか |
| `check_lockout` | 失敗カウントが5以上で `429` が返るか |
| `record_login_failure` | カウントが正しくインクリメントされるか |
| `clear_lockout` | カウントが削除されるか |

### 認証APIテスト（`api/auth.py`）

| エンドポイント | テスト内容 |
|---|---|
| `POST /register` | 正常登録でユーザーが作成されるか |
| `POST /register` | 重複メールアドレスで `409` が返るか |
| `POST /login` | 正常ログインでhttpOnly Cookieがセットされるか |
| `POST /login` | 誤パスワードで `401` が返るか |
| `POST /login` | 存在しないメールアドレスで `401` が返るか（エラーメッセージが誤パスワードと同一か） |
| `POST /login` | 5回連続失敗後に `429` が返るか |
| `POST /login` | ログイン成功で失敗カウントがリセットされるか |
| `GET /me` | 認証済みで自分のメールアドレス等が返るか |
| `GET /me` | 未認証で `401` が返るか |
| `POST /logout` | Cookieが削除されるか |

### 分析APIテスト（`api/analysis.py`）

| エンドポイント | テスト内容 |
|---|---|
| `POST /upload` | 未認証で `401` が返るか |
| `POST /upload` | 対応外ファイル形式（例: `.txt`）で `400` が返るか |
| `POST /upload` | 50MB超のファイルで `400` が返るか |
| `POST /upload` | 有効なWAVファイル・認証済みで `200` と `analysis_id` が返るか |
| `GET /{id}` | 自分の分析結果が正しく返るか |
| `GET /{id}` | 他ユーザーの結果IDで `403` が返るか |
| `GET /{id}` | 存在しないIDで `404` が返るか |
| `GET /{id}` | 未認証で `401` が返るか |
| `GET /user/statistics` | 認証済みで統計データが返るか |
| `GET /user/statistics` | 未認証で `401` が返るか |

---

## 開発環境セットアップ方針

### テスト用アカウント（シードスクリプト）

DBリセット時に備えて、テスト用アカウントを作成するシードスクリプト（`backend/seed.py`）を用意する方針。**DBを初めてリセットしたタイミングで作成する**（現時点では未作成）。
